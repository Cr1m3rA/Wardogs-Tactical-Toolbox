# WARDOGS 迫击炮计算器 · 微信小程序开发计划

> ⚠️ **状态：已放弃（2026-09-15 决定）。** 结论：公开发布必须有 ICP 备案域名，经评估不备案，
> 故不做小程序，转为把网页版（`mortar-map.html`）增强为「战术工具箱」。本文档留档，
> 其中瓦片分发、版权风险、UI 同步策略的分析对未来任何移动端/分发形态仍有参考价值。

> 版本 v1.0 · 2026-09-15
> 参考实现：`D:\WardogsArtillery\mortar-map.html`（地图版）与 `mortar-calculator.html`（纯坐标版）
> 原则：**网页版是唯一事实来源（source of truth），小程序的 UI、布局、文案、配色全部跟它同步。**

---

## 1. 目标与范围

把现有网页版计算器移植为微信小程序，功能对齐：

| 模块 | 网页版已有 | 小程序范围 |
|---|---|---|
| 解算（两点 → 距离/方位/密位） | ✅ | ✅ 必做 |
| 预判（位置+方位+密位 → 落点） | ✅ | ✅ 必做 |
| 修正（弹坑 → 修正口令） | ✅ | ✅ 必做 |
| 地图标点（真实地形 + 缩放 + 拖动） | ✅ | ✅ 必做 |
| 三张地图（Bakurani / Ozeti / Zestafona） | ✅ | ✅ 必做 |
| 地标 POI（塔楼/出生点/商人） | ✅ | ✅ 必做 |
| 本地持久化（点位、设置） | localStorage | wx.storage |
| 分享战位给队友 | — | ➕ 增值项 |

不做：账号体系、云同步、社区功能、排行榜。

---

## 2. 硬约束（决定架构，先看这个）

### 2.1 包体积 —— 瓦片不可能进包
- 小程序主包 **2 MB**，整包（含分包）上限 **30 MB**。
- 三张图全量瓦片 **65,535 张 / 1.65 GB**，放不进去，**必须走网络加载**。
- 分包只能装代码，不能拿来囤图片资源（图片放分包也不计入 2 MB 主包，但整包 30 MB 依然装不下）。

### 2.2 合法域名 —— 最大门槛
- `wx.request` / `wx.downloadFile` / `<image>` 远程图，都要求 **HTTPS + ICP 备案** 的域名，且在小程序后台配置白名单。
- 社区 CDN `assets.wardogs-artillery.com` 是境外域名，**没有备案，不能配置**。
- 也就是说：**想公开发布，就必须自建一个已备案域名的瓦片服务**。

三条路线：

| 方案 | 说明 | 成本 | 适用 |
|---|---|---|---|
| A. 体验版 + 关闭域名校验 | 开发者工具勾选"不校验合法域名"，手机开调试模式 | 0 元，当天能用 | **只给自己/朋友用 ← 推荐起步** |
| B. 自建 COS + CDN | 把已下载的 1.65 GB 瓦片传到腾讯云 COS，挂已备案域名 | 存储 ≈ 0.1 元/GB/月；CDN 流量按量 | 正式发布 |
| C. 云托管/云函数反代 | 云函数中转到社区 CDN | 需备案域名 + 出网流量费 | 不推荐（受制于人） |

**结论：先走 A 把功能做出来（瓦片直连社区 CDN），要不要公开上线、上 B 的成本，等功能验证完再决定。**

### 2.3 类目与资质
- 选 **工具 → 效率/计算器** 类目，个人主体即可。
- **不使用** `wx.getLocation`、不使用 `<map>` 组件，就不触发"地图/定位"类目资质要求——我们是自绘 canvas 瓦片图，与腾讯地图无关。
- 隐私协议：只用到本地存储，`privacy` 声明里勾"本地存储"即可。

### 2.4 版权与上架风险 ⚠️
- 射表、地标、底图影像来自 `apollyon-sys/wardogs-calculator`；**代码 MIT，底图影像是 WARDOGS 游戏资产、非 MIT**。
- 公开上架一个"官方游戏外工具"有被投诉下架的风险。缓解：
  - "关于"页明确标注：社区测量值、非 Bulkhead 官方、影像版权归原游戏、仅供个人参考。
  - 不使用游戏 Logo / 美术素材，界面完全自绘。
  - 名称避开"WARDOGS"商标词，可用"迫击炮射表助手"这类中性名。
- 如果风险不可接受 → 只发体验版（最多 15 个体验成员），不走审核。

### 2.5 WebP 支持需真机验证
- 瓦片是 WebP。iOS/Android 微信的 `<image>` 与 canvas 2d 理论上都支持，但 **必须第一周就在真机上验证**，不通过就服务端转 JPEG/PNG（COS 图片处理一条配置的事）。

---

## 3. 技术选型

| 选项 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **原生小程序** | 无构建链；canvas 2d 接口最全；现有代码是零依赖原生 JS，核心算法可直接复制 | 只能上微信 | ✅ **推荐** |
| Taro 3 + React | 将来可多端（支付宝/抖音） | 多 150KB 运行时；canvas 触摸事件封装有坑；调试链路长 | 仅当明确要多端 |
| uni-app | Vue 生态 | 同上 | 不考虑 |

现有 `mortar-map.html` 里的核心是纯函数、无 DOM 依赖，**可以直接搬**：

```
core/
  ballistics.js   TBL 射表、rangeToMil、milToRange、MOA50 散布
  coords.js       solveVector、unitToXY、parsePair（含 1 格=100m 换算）
  maps.js         MAPS 配置（bounds / tiles / markers，三张图全量内嵌，仅几 KB）
```

UI 层重写（WXML/WXSS），逻辑层复用，这是本次移植工作量的大头所在——**地图画布是唯一需要真正"重写"的部分**。

---

## 4. 地图画布设计（核心难点）

### 4.1 与网页版完全一致的坐标系统
沿用网页版的四个函数，一比一移植：

```
w2s / s2w      世界坐标 ↔ 屏幕坐标（S.view = {cx, cy, scale}）
tileZoom()     ceil(log2(boundsWidth × scale / 256))，夹在 minZoom~maxZoom
瓦片寻址        {base}/tiles/{map}/zoom_{z}/{x}_{y}.webp
```

### 4.2 手势
| 手势 | 行为 | 实现 |
|---|---|---|
| 单指拖动 | 平移视野 | touchmove 增量改 S.view.cx/cy |
| 双指捏合 | 缩放（以两指中点为锚） | 两指距离比值 → scale，锚点世界坐标保持不动（网页版已解决锚点漂移，逻辑照搬） |
| 单指点按 | 放置/选中点位 | 与网页版 13px 命中半径一致 |
| 双击 | 快速放大一级 | tap 间隔 < 300ms |

### 4.3 瓦片加载与缓存
- 网络瓦片：`image` 标签池 + `canvas.drawImage`（与网页版 Image 对象池同思路）。
- 本地缓存（方案 A 下可选，方案 B 下必做）：
  - `wx.getFileSystemManager()` 把瓦片写到 `wx.env.USER_DATA_PATH`，下次优先读本地。
  - **用户目录上限 200 MB** → 缓存策略：z0–z5 全图（约 47 MB/图，三张 141 MB）+ z6/z7 只缓存最近访问的视口，LRU 淘汰。
  - 缓存 key：`{map}/zoom_{z}/{x}_{y}.webp`，直接对应远端路径。
- 离线兜底：与网页版一致——瓦片加载失败就只画网格和标点，并给"底图离线"提示条。

---

## 5. UI / 布局同步策略 ⭐

现有网页版的结构直接映射到小程序组件，视觉规格逐项对齐：

| 网页版 | 小程序 |
|---|---|
| `<header class="top">` | 自定义导航栏 + 标题 |
| 三模式 `.tab` | 顶部 segmented control（自定义，非 tabBar，保证三模式一键切换体验一致） |
| 左侧表单卡片 `.card` | 上半屏卡片（小程序改为**上下布局**：地图在上、表单在下——横屏网页的左右结构在竖屏手机上不成立） |
| `.kv` 数据行 / `.cmd` 口令条 / `.status` 提示条 | 同名组件，颜色字号一致 |
| 图例（炮位/目标/弹坑/落点/684m/132m） | 地图右下角浮层，原样保留 |
| 「底图 自动/本地/在线」切换 | 设置页三选一，逻辑同源 |

颜色 token（网页版已定，直接搬）：

```
炮位 #2f6f4f   目标 #a3322c   弹坑 #a8621a   落点 #2a5f9e
网格 rgba(28,32,36,.13)   卡片底 #fff   页面底 #f4f5f7
```

> 手机竖屏只做一处结构性调整：**地图在上（约 45% 高）、操作区在下（可滚动）**；
> 点位仍支持地图直点，表单与地图双向联动（网页版已有的 `syncFromState` 逻辑照搬）。

---

## 6. 里程碑

| 阶段 | 内容 | 产出 | 工作量 |
|---|---|---|---|
| **M0 决策** | 主体类型、是否公开上线、方案 A/B | 定稿 | 0.5 天 |
| **M1 核心移植** | `core/`三模块 + 单元测试（网页版 31 条用例直接搬）；三模式表单页 | 无地图的计算器可用 | 2 天 |
| **M2 地图画布** | 瓦片加载/缩放/拖动/标点/POI/图例/离线提示 | 地图版可用（方案 A 直连 CDN） | 4 天 |
| **M3 数据与缓存** | 瓦片本地 LRU 缓存；点位 localStorage 迁移与导入导出 | 离线可用 | 2 天 |
| **M4 同步走查** | 与网页版逐屏对比配色/文案/数字格式；真机（iOS+Android）各过一遍 | UI 一致性验收 | 1 天 |
| **M5 分享与收尾** | 分享卡片（点位参数encode到 path）；隐私协议；关于页 | 提审包 | 1 天 |
| **M6（可选）** | 若走方案 B：COS 上传脚本 + 域名配置 + 切换瓦片源 | 正式发布 | 2 天 + 备案等待 |

**合计：约 12 个工作日**（方案 A / 体验版）；
走方案 B 追加 2 天 + ICP 备案等待（个人备案 1~3 周，是唯一无法压缩的日历时间）。

---

## 7. 风险清单

| 风险 | 等级 | 对策 |
|---|---|---|
| 社区 CDN 无备案，正式版无法直连 | 高 | 方案 B（自建 COS）；或止步体验版 |
| 游戏资产版权导致下架 | 中 | 中性命名 + 免责声明 + 不用官方美术；必要时止步体验版 |
| WebP 真机兼容 | 低 | M2 第一天就真机验证；兜底转 JPEG |
| 200MB 缓存上限 | 低 | 分级缓存策略（z0–z5 全量 + 高层级 LRU） |
| 社区标定数据本身有偏差（Ozeti 约 162m 偏移待确认、Zestafona 未实测） | 中 | 与网页版共用同一份 MAPS，数据修正只改一处；"关于"页注明置信度 |
| 50 MOA 散布导致首发偏差 | 说明性 | 界面沿用网页版提示文案，不做算法改动 |

---

## 8. 需要你拍板的三件事

1. **只给自己用，还是要公开上线？**（决定走方案 A 还是 B，也决定版权风险是否成立）
2. **小程序主体是个人还是企业？**（个人主体不能开 web-view、类目受限，但本项目都用不到）
3. **手里有没有已备案域名？**（有 → 方案 B 成本极低；没有 → 备案要等 1~3 周）

确认后我从 M1 开干，先把 `core/` 三模块和三模式表单页做出来。
